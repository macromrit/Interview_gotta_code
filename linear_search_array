def linear_search(x: list, y: int)->int:
    track = 0
    for i in x:
        if i==y:
            break
        else: pass
        track+=1
    
    ans = track if y in x else -1
    return ans

print(linear_search(x=[0 ,1,2,3,4,5,6,7], y=0))
